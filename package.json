{
  "name": "react-express-scaffold",
  "version": "2.0.0",
  "description": "Production-ready full-stack JavaScript application with React, Express, and PostgreSQL",
  "private": true,
  "scripts": {
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm ci && npm run build",
    "build:server": "cd server && npm ci && npm run build",
    "dev": "docker-compose -f docker-compose.dev.yml up --build",
    "dev:detached": "docker-compose -f docker-compose.dev.yml up -d",
    "prod": "docker-compose up --build",
    "prod:detached": "docker-compose up -d",
    "down": "docker-compose down",
    "down:volumes": "docker-compose down -v",
    "logs": "docker-compose logs -f",
    "clean": "rm -rf dist client/dist server/dist"
  },
  "keywords": ["react", "express", "postgresql", "docker", "vite"],
  "license": "MIT"
}
